import * as React from "react"
import PageTemplate from "../../../page-components/page-template/PageTemplate"

const Visitor = props => {
  return <PageTemplate title={"Visitor"}>visitor</PageTemplate>
}

export default Visitor
